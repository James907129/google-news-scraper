3. The data will be saved in the `output.json` file in the `data/` folder.

## Configuration

Edit the `settings.example.json` file to set the query parameters such as start date, end date, and search query.